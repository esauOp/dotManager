<%= form_for(@task_status, remote: true) do |f| %>
  <% if @task_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_status.errors.count, "error") %> prohibited this task status from being saved:</h2>

      <ul>
      <% @task_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Save', :class => "btn btn-info" %>
  </div>
<% end %>

