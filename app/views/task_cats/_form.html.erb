<%= form_for(@task_cat) do |f| %>
  <% if @task_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_cat.errors.count, "error") %> prohibited this task category from being saved:</h2>

      <ul>
      <% @task_cat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>